<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="СПП лаб. 3" Height="450" Width="700">
    <Window.DataContext>
        <local:ViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="3*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <Button Grid.Row="0" Margin="10 20" Command="{Binding GetDir}">Выбрать каталог</Button>
            <StackPanel Grid.Row="1" VerticalAlignment="Center" Margin="10 20">
                <TextBlock HorizontalAlignment="Center">Количество потоков</TextBlock>
                <Slider IsSnapToTickEnabled="True" Minimum="1" Maximum="128" Name="ThreadSlider" Value="{Binding ThreadAmount}"></Slider>
                <TextBlock Text="{Binding ElementName=ThreadSlider, Path=Value}" HorizontalAlignment="Center"></TextBlock>
            </StackPanel>
            <Button Grid.Row="2" Margin="10 20" Command="{Binding StartScan}">
                <TextBlock TextWrapping="Wrap" TextAlignment="Center">Начать сканирование</TextBlock>
            </Button>
            <Button Grid.Row="3" Margin="10 20" Command="{Binding CancelScan}">
                <TextBlock TextWrapping="Wrap" TextAlignment="Center">Остановить сканирование</TextBlock>
            </Button>
        </Grid>
        <Grid Grid.Column="1" Margin="10 20">
            <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
                <RowDefinition Height="10*"></RowDefinition>
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock VerticalAlignment="Center" Margin="10 0">Каталог:</TextBlock>
                <TextBox Margin="10 0" Text="{Binding Dir}" MinHeight="20" Width="Auto"></TextBox>
            </StackPanel>
            <TreeView Grid.Row="1" ItemsSource="{Binding Files}">
                <TreeView.ItemContainerStyle>
                    <Style TargetType="TreeViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Style.Resources>
                            <Style TargetType="Border">
                                <Setter Property="Grid.ColumnSpan" Value="2" />
                            </Style>
                        </Style.Resources>
                    </Style>
                </TreeView.ItemContainerStyle>
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Nodes}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1*"></ColumnDefinition>
                                <ColumnDefinition Width="20*"></ColumnDefinition>
                                <ColumnDefinition Width="6*"></ColumnDefinition>
                                <ColumnDefinition Width="4*"></ColumnDefinition>
                            </Grid.ColumnDefinitions>
                            <Image Source="{Binding Icon}" Grid.Column="0" Width="20" Height="20"></Image>
                            <TextBlock Text="{Binding Name}" Grid.Column="1"></TextBlock>
                            <TextBlock Text="{Binding Size}" Grid.Column="2"></TextBlock>
                            <TextBlock Text="{Binding StringFormat={}{0:F2}%, Path=Percent}" Grid.Column="3"></TextBlock>
                        </Grid>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>
        </Grid>
    </Grid>
</Window>
